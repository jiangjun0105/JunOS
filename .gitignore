# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# generated ElevenLabs upload bundle (scripts/build-agent-bundle.mjs) — regenerate, don't commit
/dist

# vercel
.vercel

# misc
.DS_Store
*.pem
*.tsbuildinfo
next-env.d.ts

# debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files — never commit secrets (a committed .env.example template is fine)
.env
.env.*
!.env.example

# knowledge base: verbatim interview transcripts. Local only — they are Jun's
# unedited words and name the stealth startup, and this repo is public. The
# refined/ and agent/ derivatives ARE tracked; see kb/README.md.
/kb/raw/
